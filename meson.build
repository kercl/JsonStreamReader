project('jsonstream', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3'])

inc_directory = include_directories('include')

inc_files = []
src_files = []

subdir('include')
subdir('src')
subdir('test')
